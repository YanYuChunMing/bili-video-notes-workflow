services:
  bili-video:
    build: .
    image: bili-video-notes:latest
    container_name: bili-video-notes
    profiles: [""]
    volumes:
      - ./outputs:/app/outputs
      - ./downloads:/app/downloads
      - ./logs:/app/logs
      - ./secrets:/app/secrets
      - ./links.txt:/app/links.txt:ro
      - ./links_with_images.txt:/app/links_with_images.txt:ro
      - ./config.toml:/app/config.toml:ro
    environment:
      - PYTHONUNBUFFERED=1
      - BILI_SECRET_DB=/app/secrets/local_secrets.sqlite3
      - BILI_SECRET_PASSPHRASE=${BILI_SECRET_PASSPHRASE:-}
    command: ["--task", "basic_test"]

  bili-video-web:
    build:
      context: .
      dockerfile: Dockerfile.web
    image: bili-video-notes:web-latest
    container_name: bili-video-notes-web
    profiles: ["web"]
    ports:
      - "8501:8501"
    volumes:
      - ./outputs:/app/outputs
      - ./downloads:/app/downloads
      - ./logs:/app/logs
      - ./secrets:/app/secrets
      - ./links.txt:/app/links.txt:ro
      - ./links_with_images.txt:/app/links_with_images.txt:ro
      - ./config.toml:/app/config.toml
    environment:
      - PYTHONUNBUFFERED=1
      - BILI_SECRET_DB=/app/secrets/local_secrets.sqlite3
      - BILI_SECRET_PASSPHRASE=${BILI_SECRET_PASSPHRASE:-}

  bili-video-gpu:
    build:
      context: .
      dockerfile: Dockerfile.gpu
    image: bili-video-notes:gpu-latest
    container_name: bili-video-notes-gpu
    profiles: ["gpu"]
    volumes:
      - ./outputs:/app/outputs
      - ./downloads:/app/downloads
      - ./logs:/app/logs
      - ./secrets:/app/secrets
      - ./links.txt:/app/links.txt:ro
      - ./links_with_images.txt:/app/links_with_images.txt:ro
      - ./config.toml:/app/config.toml:ro
    environment:
      - PYTHONUNBUFFERED=1
      - NVIDIA_VISIBLE_DEVICES=all
      - BILI_SECRET_DB=/app/secrets/local_secrets.sqlite3
      - BILI_SECRET_PASSPHRASE=${BILI_SECRET_PASSPHRASE:-}
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    command: ["--task", "basic_test"]
